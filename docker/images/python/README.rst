A simple container that installs Python 3 and invoke in debian:buster-slim
image.

Running::

    sudo docker run -it ejplatform/python


Building::

    sudo docker build . -t ejplatform/python:latest
    sudo docker push ejplatform/python:latest

