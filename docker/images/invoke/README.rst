A simple container that installs Python 3 and invoke in debian:buster-slim
image.

Running::

    sudo docker run -it ejplatform/invoke


Building::

    sudo docker build . -t ejplatform/invoke:latest
    sudo docker push ejplatform/invoke:latest

