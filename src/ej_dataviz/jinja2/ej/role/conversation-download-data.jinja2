<div class="margin-t3">
    <span class="bold">{{ title|default(_('Downloads: ')) }}</span>
    <ul class="list-reset margin-x3">
        {%- for format, name, url in formats -%}
            <li class="margin-0"><a href="{{ url }}">
                <span class="brand bold">{{ name }}</span></a>
                {%- if format == 'csv' %}
                    - {{ _('Text-based table format that can be imported to Excel and other spreadsheets.') }}
                {% elif format == 'json' %}
                    - {{ _('Simple data format expressed as a list of dictionaries. (For advanced users)') }}
                {% elif format == 'msgpack' %}
                    - {{ _('Binary faster alternative to JSON.') }}
                {% endif %}
            </li>
        {% endfor %}
    </ul>
</div>
