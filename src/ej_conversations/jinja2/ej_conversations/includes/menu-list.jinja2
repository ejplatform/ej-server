<nav title="{{ _('Conversations') }}">
    <h1>{{ _('Conversations') }}</h1>
    <ul>
        <li><a href="{{ url('conversation:list') }}">{{ _('Conversations') }}</a></li>
        {%- if request.user.has_perm('ej.can_promote_conversations') %}
            <li><a href="{{ url('conversation:create') }}">{{_('New Conversation') }}</a></li>
        {% endif %}
        {% for link in page_menu.links('conversations:list', request) %}
            <li>{{ link }}</li>
        {% endfor %}
    </ul>

</nav>
{{ page_menu.ABOUT() }}
{{ page_menu.ACCESSIBILITY() }}
