{% macro toast(idtoast, title, content, img) %}
    <div id="{{ idtoast }}" class="Toast">
        <div>
            <img src="{{ img }}">
        </div>
        <div id="text">
            <h3>{{title}}</h3>
            <p>{{content}}</p>
        </div>
    </div>
{% endmacro %}
