The management command `python manage.py loadpages` will scan a folder searching
for Markdown and HTML files and store them on the database as flatpages. Use
this to store pages for your installation as code.
